[project]
name = "ollama-app"
version = "0.1.0"
description = "A RAG app using Groq and HF API"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "groq>=0.11.0",
    "requests",
    "streamlit>=1.30.0",
]
